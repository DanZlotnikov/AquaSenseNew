# Evaluation configuration for the weight model.
# All paths are relative to the project root.

data_dir: data/v4/
run_tag: v4_weight
batch_size: 64

# sigmoid(logit) >= threshold -> fish present
threshold: 0.5

# Directory where per-run result JSON files and figures are written
results_dir: results/weight/
