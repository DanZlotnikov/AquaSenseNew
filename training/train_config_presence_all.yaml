data_dir: data/presence_all/

batch_size: 64

max_epochs:   400
lr:           1.0e-3
weight_decay: 1.0e-4

pos_weight:           2.0
huber_delta:          1.0
loss_weight_presence: 1.0
loss_weight_length:   0.0

length_noise_std: 0.0

lr_patience:              20
early_stopping_patience:  60

checkpoint_dir: checkpoints/fish_presence_model/
