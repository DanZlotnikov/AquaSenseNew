data_dir: data/carp/weight_baseline/
model_config: model/model_config_12ch_salinity.yaml

batch_size: 16
max_epochs: 300
lr: 1.0e-3
weight_decay: 1.0e-4

pos_weight: 1.0
huber_delta: 50.0
loss_weight_presence: 0.0
loss_weight_length: 1.0

length_noise_std: 30.0

lr_patience: 20
early_stopping_patience: 60

checkpoint_dir: checkpoints/carp_weight_baseline_model/
